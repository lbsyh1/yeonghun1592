컴포넌트는 대문자로 시작

```

function Add=()=>{
const name="tom"
return(
<div>
  <h1 style={ {color:"red",backgroundColor:"black"}}>///객체로 포함시켜야 한다
  {name}
  </h1>
</div>
)}

function Add=()=>{}와

function Add() {}의 차이
컴포넌트 만들때는 함수정의하듯이 하면된다 첫글자만 대문자로 하고

const hello =function(){

const hello=()=>{

<h1 className="123">이런식으로 클래스부여하고 App.css에서 작성할 수도 있다

import styles form ../hello.css
이렇게 하면 styles라는 객체가 생성된다
그럼 클래스를 사용할떄 className={style.box}이런식으로 할 수 있다
styles라는 새로 생성된 객체안에 hello.css안의 모든 클래스가 담긴다

